'use client';import {useEffect,useState} from 'react';import {apiBlob} from '@/lib/api';
export default function SecureImage({path,alt,className}:{path:string;alt:string;className?:string}){const[src,setSrc]=useState<string>('');useEffect(()=>{let url='';let live=true;apiBlob(path).then(b=>{url=URL.createObjectURL(b);if(live)setSrc(url)}).catch(()=>{});return()=>{live=false;if(url)URL.revokeObjectURL(url)}},[path]);if(!src)return <div className={className} style={{width:'100%',height:'100%',background:'linear-gradient(135deg,#eee8f5,#f8e8ef)'}}/>;return <img className={className} src={src} alt={alt}/>}
