'use client';import Link from 'next/link';import {FormEvent,useEffect,useState} from 'react';import {api} from '@/lib/api';
export default function Reset(){const[token,setToken]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');const[error,setError]=useState('');useEffect(()=>setToken(new URLSearchParams(window.location.search).get('token')??''),[]);async function submit(e:FormEvent){e.preventDefault();setError('');try{const r=await api<{message:string}>('/auth/reset-password',{method:'POST',body:JSON.stringify({token,password}),auth:false});setMsg(r.message)}catch(e){setError((e as Error).message)}}return <div className="auth-main" style={{minHeight:'100vh'}}><form className="auth-card" onSubmit={submit}><p className="eyebrow">NEW PASSWORD</p><h1>Choose a new password</h1><div className="form"><div className="field"><label>Reset token</label><input value={token} onChange={e=>setToken(e.target.value)} required/></div><div className="field"><label>New password</label><input type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></div>{msg&&<div className="form-message success">{msg}</div>}{error&&<div className="form-message error">{error}</div>}<button className="primary-btn">Update password</button></div><div className="auth-links"><Link href="/login">Return to sign in</Link></div></form></div>}
